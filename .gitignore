node_modules

# Output
.wrangler

# Local agent tooling (impeccable skill/agents/hooks, dev-server launch config).
# Deliberately untracked: it's per-machine developer tooling, not part of the site,
# and reinstallable with `npx impeccable install`.
.claude/

# Impeccable run artifacts (critique snapshots, ignore lists, config).
# Tool state, not project content.
.impeccable/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# raw camera files never ship to the browser; keep locally, do not track
*.NEF
*.nef

# Eleventy build output
_site/
